package main

import "fmt"

func main() {

	// soal nomor 1

	// const pi float64 = 3.14
	// var l, r float64
	// var d float64
	// fmt.Scan(&l)
	// r = l / pi
	// d = r / 4
	// fmt.Println(d)

	// soal nomor 2

	// var x float64
	// var hasil float64
	// fmt.Scan(&x)
	// hasil = (x*x + 1.0/x) * (x*x + 1.0/x)
	// fmt.Println(hasil)

	// soal nomor 3

	// var x float64
	// var hasil float64
	// fmt.Scan(&x)
	// hasil = ((x * x * x) + (3.0 * x)) / ((x * x * x * x) - (3.0 * x * x) + 4)
	// fmt.Println(hasil)

	// soal nomor 4

	// var x string
	// fmt.Scanf(x)
	// fmt.Println(x)

	// soal nomor 5

	// var in string
	// var a byte
	// var hasil bool
	// fmt.Scan(&in)
	// a = in[0]
	// hasil = (a == 97) || (a == 105) || (a == 117) || (a == 101) || (a == 111)
	// fmt.Println(hasil)

	// soal nomor 6

	// var in string
	// var a byte
	// var hasil bool
	// fmt.Scan(&in)
	// a = in[0]
	// hasil = (a != 97) && (a != 105) && (a != 117) && (a != 101) && (a != 111) && (a >= 97) && (a <= 122)
	// fmt.Println(hasil)

	// soal nomor 7

	// var adik, kakak int
	// var selisih int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// selisih = adik - kakak
	// hasil = (adik == kakak) || (selisih == 1) || (selisih == 5) || (selisih == -1) || (selisih == -5)
	// fmt.Println(hasil)

	// soal nomor 8

	// var adik, kakak int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// hasil = (adik == kakak) || (kakak*2 == adik)
	// fmt.Println(hasil)

	// soal nomor 9

	// var adik, kakak int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// hasil = (adik == kakak) || (kakak*3 == adik)
	// fmt.Println(hasil)

	// soal nomor 10

	// var adik, kakak int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// hasil = ((adik%2 == 0) && (kakak%2 == 0)) || ((adik%2 == 1) && (kakak%2 == 1))
	// fmt.Println(hasil)

	// soal nomor 11

	// var adik, kakak int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// hasil = (kakak*2 == adik) || (kakak*3 == adik)
	// fmt.Println(hasil)

	// soal nomor 12

	// var adik, kakak int
	// var selisih, selisih2 int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// selisih = adik - kakak
	// selisih2 = kakak - adik
	// hasil = (selisih%2 == 1) || (selisih2%2 == 1)
	// fmt.Println(hasil)

	// soal nomor 13

	// var adik, kakak int
	// var selisih, selisih2 int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// selisih = adik - kakak
	// selisih2 = kakak - adik
	// hasil = (selisih%2 == 0) || (selisih2%2 == 0)
	// fmt.Println(hasil)

	// soal nomor 14

	// var adik, kakak int
	// var selisih int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// selisih = adik - kakak
	// hasil = ((adik <= 9) && (adik >= 0) && (kakak <= 9) && (kakak >= 0)) && ((selisih == 2) || (selisih == 3) || (selisih == 5) || (selisih == 7) || (selisih == -2) || (selisih == -3) || (selisih == -5) || (selisih == -7))
	// fmt.Println(hasil)

	// soal nomor 15

	// var adik, kakak int
	// var hasil bool
	// fmt.Scan(&adik, &kakak)
	// hasil = (adik%2 == 0 && kakak%2 == 1) || (adik%2 == 1 && kakak%2 == 0)
	// fmt.Println(hasil)

	// soal nomor 16

	// var a, b, c int
	// fmt.Scan(&a, &b, &c)
	// fmt.Println(c, a, b)

	// soal nomor 17

	// var z, a, b, c, d int
	// fmt.Scan(&a, &b, &c, &d)
	// z = a
	// a = b
	// b = c
	// c = d
	// d = z
	// fmt.Println(a, b, c, d)

	// soal nomor 18

	// var d1, d2, d3 int
	// var hasil bool
	// fmt.Scan(&d1, &d2, &d3)
	// hasil = (d1%2 == 1) && (d2%2 == 1) && (d3%2 == 1)
	// fmt.Println(hasil)

	// soal nomor 19

	// var d1, d2, d3 int
	// var hasil bool
	// fmt.Scan(&d1, &d2, &d3)
	// hasil = (d1%2 == 0) && (d2%2 == 0) && (d3%2 == 0)
	// fmt.Println(hasil)

	// soal nomor 20

	// var d1, d2, d3 int
	// var hasil bool
	// fmt.Scan(&d1, &d2, &d3)
	// hasil = (d1%2 == 1) || (d2%2 == 1) || (d3%2 == 1)
	// fmt.Println(hasil)

	// soal nomor 21

	// var d1, d2, d3 int
	// var hasil bool
	// fmt.Scan(&d1, &d2, &d3)
	// hasil = d1%2 == 0 || d2%2 == 0 || d3%2 == 0
	// fmt.Println(hasil)

	// soal nomor 22

	// var d1, d2, d3 int
	// var hasil bool
	// fmt.Scan(&d1, &d2, &d3)
	// hasil = (d2-d1 == 1) && (d3-d2 == 1)
	// fmt.Println(hasil)

	// soal nomor 33

	// var in string
	// var a, b, c, d byte
	// var hasil bool
	// fmt.Scan(&in)
	// a = in[0]
	// b = in[1]
	// c = in[2]
	// d = in[3]
	// hasil = a < b && b < c && c < d
	// fmt.Println(hasil)

	// nomor 4 CLO 1

	var in, a, b, c, in2 int
	var hasil bool
	fmt.Scan(&in)
	a = in % 100 % 10 * 100
	b = in % 100 / 10 * 10
	c = in / 100
	in2 = a + b + c
	hasil = in2 == in
	fmt.Println(hasil)
	fmt.Println(in2)

}
