package main

import (
	"fmt"
)

func main() {
	// var x int
	// var d1, d2, d3 int

	// fmt.Println("input number")
	// fmt.Scan(&x)
	// d1 = x % 100 % 10
	// x = x / 10
	// d2 = x % 10
	// x = x / 10
	// d3 = x / 1

	// fmt.Println(d1, d2, d3)

	var r float64
	var f float64

	fmt.Println("input r")
	fmt.Scan(&r)
	f = 4 * (22 / 7) * (r) * (r)
	fmt.Println(f)

	// var x int
	// var rb1, rb5, rb10 int

	// fmt.Println("input nominal uang :")

	// fmt.Scan(&x)

	// rb10 = x / 10000
	// x = x % 10000
	// rb5 = x / 5000
	// x = x % 5000
	// rb1 = x / 1000

	// fmt.Println(rb10, "lembar 10 ribu,", rb5, "lembar 5 ribu,", rb1, "lembar seribu", "rupiah")

	// var p, l, x, y int
	// var g, f int

	// fmt.Scan(&p, &l)
	// x = 2 * p
	// y = 2 * l
	// f = x + y
	// g = p * l

	// fmt.Println(f, g)

	// var x, y, z int
	// var hargaJual1, hargaJual2, hargaJual3 float64

	// fmt.Println("masukkan 3 harga beli")
	// fmt.Scan(&x, &y, &z)

	// hargaJual1 = float64(x) + (float64(x) * 5 / 100)
	// hargaJual2 = float64(y) + (float64(y) * 5 / 100)
	// hargaJual3 = float64(z) + (float64(z) * 5 / 100)

	// fmt.Println(hargaJual1, hargaJual2, hargaJual3)

}
